@model Badges.Models.Shared.ImageModel
@if (Model == null)
{
    <img id="profile-picture" />
}
else
{
    <img id="profile-picture" src="@Model.Url" height="@Model.Height" width="@Model.Width" alt="@Model.Alt"></img>
}